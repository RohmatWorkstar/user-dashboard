import UserList from "../components/UserList";

const HomePage = () => {
    return (
        <div className="bg-gradient-to-r from-blue-500 to-purple-500 p-4 min-h-screen">
            <UserList />
        </div>
    );
};

export default HomePage;
